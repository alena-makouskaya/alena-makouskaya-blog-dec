import styled from "styled-components";
import { lightTheme } from "../../../styles/colors";

export const StyledHeader = styled.header`
padding: 24px 0;

  border-bottom: 1px solid ${lightTheme.border.border};
`;

